Very simple HTTP service exposing PowerShell, just call it like this http://srv:port/?cmd=get-service | ? {$_.Name -like "*Time*"}

To run pshttp (listening on port 35) use this command:
PowerShell -NonInteractive -ExecutionPolicy unrestricted -file server.ps1 35